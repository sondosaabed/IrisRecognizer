<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/iris_recognizer_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iris_recognizer_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IrisRecognizer"
        tools:targetApi="31"
        >
        <activity
            android:name="com.irisrecognizer.activities.IntroductoryActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AuthenticationActivity" android:exported="false"></activity>
        <activity android:name=".activities.EnrollmentActivity" android:exported="false"></activity>
    </application>
</manifest>